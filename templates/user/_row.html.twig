<tr class="even:bg-gray-700 odd:bg-gray-600">
    <td class="px-6 py-4 whitespace-nowrap">{{ user.id }}</td>
    <td class="px-6 py-4 whitespace-nowrap">{{ user.name }}</td>
    <td class="px-6 py-4">{{ user.firstName }}</td>
    <td class="px-6 py-4 whitespace-nowrap">{{ user.lastName }}</td>
    <td class="px-6 py-4 whitespace-nowrap">
        <a href="{{ path('app_user_show', {'id': user.id}) }}" class="text-blue-400 hover:text-blue-600">show</a>
        <a href="{{ path('app_user_edit', {'id': user.id}) }}" class="ml-4 text-yellow-400 hover:text-yellow-600">edit</a>
    </td>
</tr>